import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ella Travel | Premium Travel Experience",
  description: "Discover extraordinary travel experiences with Ella Travel. Luxury tours, exotic destinations, and unforgettable adventures await you.",
  keywords: "travel, tourism, luxury travel, tours, vacation",
  openGraph: {
    title: "Ella Travel | Premium Travel Experience",
    description: "Discover extraordinary travel experiences with Ella Travel.",
    type: "website",
    url: "https://ella-travel.com",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
